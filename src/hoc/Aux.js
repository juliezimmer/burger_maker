// This HOC exists to wrap adjacent JSC elements in Layout.js or wherever it is imported.
const aux = (props) => props.children;

export default aux;

// there is no JSX used in this file, so no need to import React.
